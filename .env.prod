# .env.prod

# --- 原有配置 ---
DRIVER=~fastapi+~websockets
LOCALSTORE_USE_CWD=true

# --- 新增：WebSocket 服务端配置 ---
HOST=127.0.0.1       # 监听所有网络接口，让 go-cqhttp 能连接进来
PORT=3921        # 监听端口，需与 go-cqhttp 中配置的端口完全一致